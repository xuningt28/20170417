#!/usr/bin/perl -w
use Data::Dumper;
   my %hash=('a'=>1,'b'=>2);
   open (FILE, "> hash_file")    or die "can't open tvinfo: $!";
   print FILE D;
   close FILE         or die "can't close tvinfo: $!";
